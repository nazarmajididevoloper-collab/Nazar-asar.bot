# Updated render.yaml

# Correct syntax without gitCloneSubpath
services:
  web:
    type: web
    name: "Your Service Name"
    env: "python"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    autoDeploy: true
